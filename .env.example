PRODUCTION=True

# (optional if passing )
# DATABASE_URL=database_url 

POSTGRES_USER=database_user
POSTGRES_PASSWORD=database_password
POSTGRES_DB=database_name
POSTGRES_HOST=db    # for docker-compose setup
POSTGRES_PORT=5432